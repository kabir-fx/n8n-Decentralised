# MongoDB connection string
MONGO_URL=mongodb://localhost:27017/n8n-decentralised

# JWT secret for authentication (min 16 characters)
JWT_SECRET=your-secret-key-here-min-16-chars

# Server port (optional, defaults to 2099)
PORT=2099

# Environment (optional, defaults to development)
NODE_ENV=development